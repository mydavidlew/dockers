# This config file contains all config fields with comments
# NOTE: this is not a particularly useful config file
#
# (run) kubectl apply -f kubernetes/apache-config.yaml
# (run) kubectl rollout status deployment/mywebserver
# (run) kubectl edit deployment.v1.apps/mywebserver
# (run) kubectl rollout history deployment.v1.apps/mywebserver
# (run) kubectl expose deployment mywebserver --type=ClusterIP --name=mywebserver --port=8081  --target-port=80
# (run) kubectl port-forward svc/mywebserver 8081:8081
#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mywebserver
  namespace: default
  labels:
    k8s-app: mywebserver
spec:
  replicas: 2
  selector:
    matchLabels:
      k8s-app: mywebserver
  template:
    metadata:
      labels:
        k8s-app: mywebserver
    spec:
      containers:
      - name: apache2
        image: httpd
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
      restartPolicy: Always
      terminationGracePeriodSeconds: 30